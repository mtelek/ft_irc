#ifndef HELPER_HPP
#define HELPER_HPP

class helper 
{
    private:
    
    public:
        helper();
        helper(const helper& other);
        helper& operator=(const helper& other);
        ~helper();
};

#endif
